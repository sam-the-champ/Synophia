# Canonical list of environment variables for the synophia Studio.
# Copy to .env.local and fill in real values. Never commit .env.local.
# The Sanity CLI only reads env vars prefixed SANITY_STUDIO_.

SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=
